# Hail Mary

Hail Mary — мобильный веб-сервис для школьников 8–11 классов, который помогает собрать профиль абитуриента, подобрать подходящие университеты и получить пошаговый план подготовки к поступлению за рубеж.

Сайт: https://mmaaiidd.github.io/Hail-Mary/

## Задача

Поступление за рубеж состоит из большого количества связанных решений. Абитуриенту необходимо выбрать направление и страну, оценить свой академический профиль, подобрать университеты, разобраться с экзаменами, документами, дедлайнами, бюджетом и дополнительными активностями.

Эта информация обычно находится в разных источниках, а подборки университетов не всегда учитывают конкретную ситуацию ученика. В результате абитуриент получает много информации, но не понимает, что подходит именно ему и какое действие необходимо выполнить следующим.

Задача Hail Mary — собрать основные этапы поступления в одном сервисе и превратить данные пользователя в понятный маршрут подготовки.

## Решение

Пользователь проходит последовательную анкету, в которой указывает свои интересы, профильный предмет, GPA, предпочтительные страны, сроки поступления, бюджет и другие параметры.

На основе этих данных сервис:

- проводит первичную диагностику профиля;
- определяет сильные стороны, пробелы и ограничения;
- формирует подборку из шести университетов;
- объясняет соответствие каждого варианта профилю пользователя;
- разделяет варианты на ambitious, balanced и safer;
- позволяет сравнить до трёх университетов;
- предлагает направления для развития extracurricular-профиля;
- формирует персональный roadmap подготовки;
- показывает конкретные следующие действия.

Дополнительно на лендинге реализована интерактивная карта университетов, а в разделе «Ресурсы» собраны внешние источники, которые пользователь может использовать для самостоятельной проверки информации.

## Пользовательский сценарий

Основной путь пользователя выглядит следующим образом:

Лендинг -> Анкета -> Диагностика -> AI-рекомендации -> Сравнение университетов -> Roadmap -> Следующие действия

Профиль и полученный результат сохраняются локально, поэтому после перезагрузки страницы пользователь может продолжить работу с уже заполненными данными.

## Готовые компоненты

На текущем этапе реализованы:

- адаптивный лендинг;
- интерактивная карта мира с университетскими маркерами;
- многошаговая анкета абитуриента;
- локальное сохранение профиля;
- первичная диагностика;
- AI-подбор университетов;
- распределение рекомендаций по категориям ambitious, balanced и safer;
- объяснение соответствия университетов профилю;
- сравнение до трёх университетов;
- рекомендации по extracurricular activities;
- персональный roadmap поступления;
- блок следующих действий;
- страницы «О нас» и «Ресурсы»;
- backend для безопасной работы с Gemini API;
- мобильная и desktop-версии интерфейса.

## Техническая справка

| Раздел | Используемые технологии |
| --- | --- |
| Frontend | React 19, TypeScript, Vite, Tailwind CSS |
| UI | Lucide React |
| Карта | react-simple-maps, TopoJSON World Atlas |
| Хранение | localStorage |
| Backend | Cloudflare Workers |
| Валидация | Zod |
| AI | Google Gemini API через `@google/genai` |
| AI-модели | настраиваемая `GEMINI_MODEL`, fallback-модели Gemini |
| Поиск источников | Google Search grounding при доступности |
| Frontend hosting | GitHub Pages |
| API hosting | Cloudflare Workers |
| Шрифт | локальный `Liter-Regular.ttf` |

## Архитектура

Упрощённая схема работы приложения:

```text
Пользователь
    ↓
React Frontend
    ↓
Анкета и первичная диагностика
    ↓
Cloudflare Worker
    ↓
Валидация данных
    ↓
Gemini API
    ↓
Рекомендации + Roadmap
    ↓
Frontend
    ↓
localStorage
```

Frontend отвечает за интерфейс, прохождение анкеты, отображение диагностики, рекомендаций, сравнения университетов и roadmap.

Профиль пользователя и результат рекомендаций сохраняются в `localStorage`.

Запрос на построение плана поступления отправляется не напрямую в Gemini, а в Cloudflare Worker. Worker проверяет входные данные, ограничивает частоту запросов, работает с кешем и только после этого обращается к Gemini API.

Благодаря этому `GEMINI_API_KEY` не включается в браузерную сборку и остаётся на стороне backend.

## AI и API

Для генерации рекомендаций используется Google Gemini API.

Основная модель задаётся через переменную окружения `GEMINI_MODEL`. Если она не указана, Worker использует модель, заданную в конфигурации приложения. Также предусмотрены резервные модели на случай ошибки основного запроса.

AI получает данные заполненной анкеты и формирует структурированный JSON-ответ.

Результат включает:

- общую стратегию поступления;
- персонализацию на основе данных анкеты;
- шесть университетов;
- две рекомендации категории ambitious;
- две рекомендации категории balanced;
- две рекомендации категории safer;
- анализ соответствия профилю;
- факторы, влияющие на поступление;
- рекомендации по extracurricular activities;
- roadmap;
- следующие действия;
- источники и disclaimer.

Roadmap состоит из пяти последовательных этапов: ближайшие действия, следующие месяцы, развитие профиля, подготовка заявки и этап подачи документов.

При включённом Google Search grounding модель может использовать поиск для проверки официальной информации. В качестве источников должны использоваться официальные страницы университетов или государственные ресурсы.

Если online-проверка недоступна, сервис переходит в model-only режим и не представляет неподтверждённые стоимость, acceptance rate, дедлайны и другие изменяемые данные как проверенные факты.

## Backend и безопасность

Cloudflare Worker выполняет несколько задач перед обращением к Gemini:

- проверяет origin запроса;
- применяет CORS;
- валидирует профиль пользователя;
- ограничивает размер входного запроса;
- применяет rate limit;
- кеширует одинаковые запросы;
- скрывает Gemini API key от frontend;
- проверяет структуру ответа модели;
- обрабатывает ошибки и fallback-модели.

Rate limit составляет до четырёх запросов в минуту на один IP.

Для кеширования профиль преобразуется в SHA-256 hash. Повторный запрос с тем же набором данных может быть получен из edge cache без повторной генерации результата.

В Gemini передаются данные анкеты, необходимые для построения рекомендаций. Имя и фамилия пользователя для генерации рекомендаций не требуются.

## Данные и источники

В проекте используются несколько типов данных:

- данные анкеты пользователя;
- TopoJSON World Atlas для отображения карты;
- координаты университетов для размещения маркеров;
- данные, полученные через Gemini;
- официальные страницы университетов и государственные источники при использовании Google Search grounding.

AI-рекомендации сами по себе не считаются официальным источником информации.

Стоимость обучения, дедлайны, визовые требования, требования конкретных программ, доступность Foundation и правила работы во время учёбы могут изменяться. Перед принятием решения пользователь должен проверить эти данные на официальном сайте университета или соответствующего государственного органа.

## Структура проекта

```text
Hail-Mary/
├── frontend/
│   └── src/
│       ├── components/
│       │   ├── landing/
│       │   ├── profile/
│       │   ├── diagnosis/
│       │   ├── admissions/
│       │   └── info/
│       └── lib/
│
├── api/
│   └── src/
│
└── .github/
    └── workflows/
```

Основные директории:

- `frontend/src/components/landing` — лендинг и карта университетов;
- `frontend/src/components/profile` — анкета абитуриента;
- `frontend/src/components/diagnosis` — первичная диагностика;
- `frontend/src/components/admissions` — рекомендации, сравнение университетов и roadmap;
- `frontend/src/components/info` — страницы «О нас» и «Ресурсы»;
- `frontend/src/lib` — работа с хранилищем, диагностика и API-клиент;
- `api/src` — Cloudflare Worker, контракты данных и prompt для Gemini;
- `.github/workflows` — автоматическая сборка и публикация frontend.

## Запуск frontend

Требуется установленный Node.js и npm.

```bash
cd frontend
npm install
npm run dev
```

После запуска приложение будет доступно по адресу:

```text
http://localhost:3000
```

Для подключения production Worker необходимо создать `frontend/.env.local`:

```text
VITE_ADMISSIONS_API_URL=https://hail-mary-admissions-api.hail-mary-admissions-api.workers.dev
```

## Запуск Worker

Перейдите в директорию API:

```bash
cd api
npm install
copy .dev.vars.example .dev.vars
```

В `.dev.vars` необходимо указать действующий:

```text
GEMINI_API_KEY=...
```

После этого Worker запускается командой:

```bash
npm run dev
```

Для использования локального Worker необходимо изменить `frontend/.env.local`:

```text
VITE_ADMISSIONS_API_URL=http://127.0.0.1:8787
```

## Проверка

### Проверка сборки frontend

```bash
cd frontend
npm run build
```

### Проверка API

```bash
cd api
npm run check
```

### Тестовый сценарий

1. Открыть главную страницу.
2. Проверить отображение лендинга и карты.
3. Начать заполнение профиля.
4. Пройти все этапы анкеты.
5. Проверить результат первичной диагностики.
6. Запросить рекомендации.
7. Дождаться формирования шести вариантов университетов.
8. Проверить разделение вариантов на ambitious, balanced и safer.
9. Открыть подробную информацию по университету.
10. Добавить университеты в сравнение.
11. Проверить сформированный roadmap.
12. Проверить блок следующих действий.
13. Перезагрузить страницу и убедиться, что профиль сохранился.
14. Проверить интерфейс на desktop и мобильном экране.

Для API отдельно проверяются обработка некорректного запроса, отсутствие API-ключа, превышение rate limit и ошибки генерации.

## Ограничения

Hail Mary является инструментом для предварительного подбора и планирования поступления, а не официальной системой университета или приёмной комиссии.

Рекомендации зависят от полноты данных, которые пользователь указал в анкете.

Категории ambitious, balanced и safer показывают относительное соответствие профилю. Категория safer не означает гарантированное поступление.

Оценка admission chance является ориентировочной оценкой на основе доступного профиля и не является статистически рассчитанной вероятностью или гарантией поступления.

Acceptance rate университета также не равен индивидуальной вероятности поступления конкретного абитуриента.

Если Google Search grounding недоступен, результат работает в model-only режиме. В таком режиме изменяемые данные необходимо дополнительно проверять по официальным источникам.

## Команда

Ермек Саржан - Project Manager / Product / Research / Testing / UX
Продуктовая логика, исследование проблемы, стратегия, тестирование и пользовательский путь.

Парман Мади - Backend / LLM / Integrations
Backend, работа с LLM, интеграции, функциональность и совместимость.

Сагат Шынгысхан - Backend / Deployment / Hosting 
Backend, функциональность, GitHub deployment и hosting.

Сейдазымов Аслан - Frontend / UI/UX / Design
Дизайн, frontend, UI/UX, визуальная система и логотип.

Фазыл Таукен - Frontend / UI/UX / Testing / Documentation
Frontend, дизайн интерфейса, тестирование и документация.

## Ссылки

Сайт: 
https://mmaaiidd.github.io/Hail-Mary/
GitHub:
https://github.com/mmAAIIdd/Hail-Mary
