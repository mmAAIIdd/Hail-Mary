---
name: verification
description: Independently review a change against acceptance criteria and meaningful checks.
---

# Verification

Use after a nontrivial implementation. Inspect current files or diff, compare behavior with acceptance criteria and run relevant tests, lint or build if available. Look for regressions and out-of-scope edits. Report blockers first, then commands/results and residual risk. Passing tests alone do not prove correctness.
